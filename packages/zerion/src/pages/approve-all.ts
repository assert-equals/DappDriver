import { Approve } from './approve';

/**
 *
 *
 * @export
 * @class ApproveAll
 * @extends {Approve}
 */
export class ApproveAll extends Approve {
  /**
   * Creates an instance of ApproveAll.
   * @memberof ApproveAll
   */
  constructor() {
    super();
  }
}
